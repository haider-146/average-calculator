# average-calculator
basically it is a project in which we use simple c++ language to code and make an average calculator. so what it does is that asks users that how many inputs they have and according to their reply asks for those many numbers and after taking input from users it calculates the average value of those integers
